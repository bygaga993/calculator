- **calc.h** — объявление класса Operation (базовый класс для всех остальных классов операций), определение всех классов операций, определение класса Calculator (сохраняет операции в вектор и возвращает результат)
- **calc.cpp** — определение всех классов
- **other_func.h** — объявление функции приведения всех символов строки к нижнему регистру
- **other_func.cpp** — определение функции приведения всех символов строки к нижнему регистру
- **main.cpp** — принимает стартовое значение и операции, потом выводит ответ
